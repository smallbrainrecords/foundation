name: Push docker image | Andromeda dev
  
on:
  push:
    branches: [develop]

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout to develop
        uses: actions/checkout@v3
        with:
          ref: develop

      - name: Set time
        run: |
          echo "date="$(date +"%Y-%m-%d")"" >> $GITHUB_ENV

      - name: Login to GAR
        uses: docker/login-action@v1
        with:
          registry: ${{ secrets.LOCATION }}-docker.pkg.dev
          username: _json_key
          password: ${{ secrets.DEV_GAR_JSON_KEY }}

      - name: Build image
        run: docker build -t smallbrains .

      - name: Push image
        run: |
          docker tag smallbrains ${{ secrets.LOCATION }}-docker.pkg.dev/${{ secrets.DEV_PROJECT_ID }}/${{ secrets.REPOSITORY }}/andromeda:${{ env.date }}

          docker push ${{ secrets.LOCATION }}-docker.pkg.dev/${{ secrets.DEV_PROJECT_ID }}/${{ secrets.REPOSITORY }}/andromeda:${{ env.date }}
